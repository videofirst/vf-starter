micronaut:
  ssl:
    port: -1    # Run on random port
  http:
    client:
      ssl:
        enabled: true
        trust-store:
          path: classpath:vf-starter-self-signed.p12
          password: changeit
          type: PKCS12